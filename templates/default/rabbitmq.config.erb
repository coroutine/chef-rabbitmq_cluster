%%%
%% Generated by Chef
%%%

[
    {kernel, [
        {inet_dist_listen_min, <%= node[:rabbitmq][:inet_dist_listen_min] %>},
        {inet_dist_listen_max, <%= node[:rabbitmq][:inet_dist_listen_max] %>}
           ]
    },
    {rabbit, [
        {cluster_nodes, {[<%= node[:rabbitmq][:cluster_disk_nodes].map{|node| "\'#{node}\'"}.join(',') %>], disc}} ]
    }
].

